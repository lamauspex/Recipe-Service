# Безопасность User Service

## 🛡️ Обзор системы безопасности

User Service реализует **многоуровневую систему безопасности** корпоративного уровня, обеспечивая комплексную защиту от различных типов атак и угроз. Система построена по принципу "Defense in Depth" с использованием современных практик безопасности.

### 🎯 Ключевые принципы безопасности

- **Zero Trust Architecture**: Проверка каждого запроса
- **Principle of Least Privilege**: Минимальные необходимые права доступа
- **Defense in Depth**: Множественные уровни защиты
- **Fail Secure**: Безопасное поведение при ошибках
- **Comprehensive Logging**: Полное логирование всех событий безопасности

## 🏗️ Архитектура безопасности

```
┌─────────────────────────────────────────────────────────────┐
│                    Perimeter Security                       │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐│
│  │   Rate Limiter  │ │   IP Blocker    │ │  CORS Config    ││
│  └─────────────────┘ └─────────────────┘ └─────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              │ passes through
┌─────────────────────────────────────────────────────────────┐
│                    Authentication Layer                     │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐│
│  │  JWT Tokens     │ │  Refresh Tokens │ │  Password Hash  ││
│  └─────────────────┘ └─────────────────┘ └─────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              │ authenticated
┌─────────────────────────────────────────────────────────────┐
│                   Authorization Layer                       │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐│
│  │  Role System    │ │  Permissions    │ │  Access Control ││
│  └─────────────────┘ └─────────────────┘ └─────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              │ authorized
┌─────────────────────────────────────────────────────────────┐
│                   Application Logic                         │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐│
│  │  Suspicious     │ │  Account        │ │  Activity       ││
│  │  Detection      │ │  Locking         │ │  Monitoring    ││
│  └─────────────────┘ └─────────────────┘ └─────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

## 🔐 Аутентификация

### JWT Token Security

#### Access Tokens
```python
class JWTService:
    def create_access_token(self, data: dict) -> str:
        # Создание JWT токена с коротким временем жизни
        to_encode = data.copy()
        expire = datetime.utcnow() + timedelta(minutes=15)  # 15 минут
        to_encode.update({"exp": expire, "type": "access"})
        
        return jwt.encode(
            to_encode, 
            self.secret_key, 
            algorithm=self.algorithm
        )
```

**Характеристики безопасности:**
- ⏱️ **Короткий TTL**: 15 минут для access tokens
- 🔒 **Подпись**: HMAC с секретным ключом
- 🎯 **Audience**: Ограничение по назначению токена
- 📝 **Тип токена**: Явное указание типа токена

#### Refresh Tokens
```python
class RefreshTokenService:
    def create_refresh_token(self, user_id: uuid.UUID, token: str, expires_at: datetime):
        # Сохранение в базе данных для отслеживания
        refresh_token = RefreshToken(
            user_id=user_id,
            token=hashlib.sha256(token.encode()).hexdigest(),  # Хеширование
            expires_at=expires_at,
            is_revoked=False
        )
        self.session.add(refresh_token)
```

**Характеристики безопасности:**
- 💾 **База данных**: Хранение в БД для отслеживания
- 🔐 **Хеширование**: SHA-256 хеширование токенов
- 🔄 **Rotation**: При каждом обновлении старый токен инвалидируется
- ⏱️ **Долгий TTL**: До 30 дней для refresh tokens

### Password Security

#### Хеширование паролей
```python
class PasswordService:
    def hash_password(self, password: str) -> str:
        # Использование bcrypt с автоматической солью
        return bcrypt.hashpw(
            password.encode('utf-8'), 
            bcrypt.gensalt(rounds=12)
        ).decode('utf-8')
    
    def verify_password(self, password: str, hashed: str) -> bool:
        # Верификация с автоматической проверкой соли
        return bcrypt.checkpw(
            password.encode('utf-8'), 
            hashed.encode('utf-8')
        )
```

**Характеристики безопасности:**
- 🧂 **Соль**: Автоматическая генерация уникальной соли
- ⏳ **Раунды**: 12 раундов bcrypt для замедления атак
- 🔒 **Алгоритм**: bcrypt - проверенный алгоритм хеширования

## 🚦 Rate Limiting

### Алгоритм ограничения запросов

```python
class RateLimiter:
    def check_rate_limit(self, ip_address: str, email: str) -> tuple[bool, Optional[str]]:
        # Проверка по IP адресу
        ip_key = f"rate_limit:ip:{ip_address}"
        ip_requests = self.redis.get(ip_key)
        
        if ip_requests and int(ip_requests) >= self.config.MAX_REQUESTS_PER_MINUTE:
            return False, "Превышен лимит запросов с IP адреса"
        
        # Проверка по email
        email_key = f"rate_limit:email:{email}"
        email_requests = self.redis.get(email_key)
        
        if email_requests and int(email_requests) >= self.config.MAX_REQUESTS_PER_EMAIL:
            return False, "Превышен лимит запросов с email"
        
        # Увеличиваем счетчики
        self._increment_counter(ip_key, 60)  # 1 минута
        self._increment_counter(email_key, 300)  # 5 минут
        
        return True, None
```

### Уровни ограничений

| Тип запроса | Лимит | Временное окно |
|-------------|-------|----------------|
| **Login**  | 5 попыток | 15 минут |
| **Register** | 3 попытки | 1 час |
| **Password Reset** | 3 попытки | 1 час |
| **API General** | 1000 запросов | 1 час |

## 🔒 Account Locking

### Система блокировки аккаунтов

```python
class AccountLocker:
    def is_account_locked(self, email: str) -> tuple[bool, Optional[str]]:
        # Проверяем текущий статус блокировки
        lock_info = self._get_lock_info(email)
        
        if lock_info and lock_info.is_locked:
            if lock_info.lock_until and datetime.utcnow() < lock_info.lock_until:
                return True, f"Аккаунт заблокирован до {lock_info.lock_until}"
            else:
                # Блокировка истекла, разблокируем
                self._unlock_account(email)
        
        return False, None
    
    def _should_lock_account(self, email: str) -> bool:
        # Проверяем количество неудачных попыток
        failed_attempts = self._get_failed_attempts(email, hours=1)
        return failed_attempts >= self.config.MAX_FAILED_ATTEMPTS
```

### Критерии блокировки

- 🚫 **5 неудачных попыток входа** за 1 час
- 🔄 **Автоматическая разблокировка** через 24 часа
- 📧 **Уведомление администратора** о блокировке
- 📝 **Логирование** всех событий блокировки

## 🌐 IP Blocking

### Детекция вредоносных IP

```python
class IpBlocker:
    def check_ip_block(self, ip_address: str) -> tuple[bool, Optional[str]]:
        # Проверяем статус IP в кэше
        if self.redis.exists(f"blocked_ip:{ip_address}"):
            return True, "IP адрес заблокирован"
        
        # Анализируем поведение IP
        if self._is_suspicious_ip(ip_address):
            self._block_ip(ip_address, duration_hours=24)
            self._notify_security_team(ip_address, "Автоматическая блокировка")
            return True, "IP адрес заблокирован за подозрительную активность"
        
        return False, None
    
    def _is_suspicious_ip(self, ip_address: str) -> bool:
        # Множественные неудачные попытки
        failed_attempts = self._get_failed_attempts_by_ip(ip_address)
        if failed_attempts >= 10:
            return True
        
        # Проверка на proxy/VPN
        if self._is_proxy_ip(ip_address):
            return True
        
        # Географическая аномалия
        if self._is_unusual_location(ip_address):
            return True
        
        return False
```

### Критерии блокировки IP

- 🚫 **10+ неудачных попыток** за час
- 🌍 **Подозрительные геолокации**
- 🔄 **Использование прокси/VPN**
- ⚡ **Сканирование портов**
- 🤖 **Бот активность**

## 🔍 Suspicious Activity Detection

### Детекция аномального поведения

```python
class SuspiciousDetector:
    def detect_suspicious_activity(self, email: str, user_agent: str) -> bool:
        # Анализ User-Agent
        if self._is_bot_user_agent(user_agent):
            return True
        
        # Проверка времени входа
        if self._is_unusual_login_time(email):
            return True
        
        # Анализ частоты запросов
        if self._is_unusual_frequency(email):
            return True
        
        # Проверка изменения паттернов
        if self._is_pattern_change(email, user_agent):
            return True
        
        return False
```

### Индикаторы подозрительной активности

| Индикатор | Описание | Действие |
|-----------|----------|----------|
| 🤖 **Bot User-Agent** | Автоматические инструменты | Блокировка |
| 🕐 **Нетипичное время** | Входы в нерабочее время | Уведомление |
| 📍 **Необычная геолокация** | Вход из новой страны | 2FA запрос |
| ⚡ **Высокая частота** | Множественные запросы | Rate limiting |
| 🔄 **Изменение паттернов** | Резкое изменение поведения | Анализ |

## 👑 Система ролей и разрешений

### Модель разрешений

```python
class Permission:
    # Базовые разрешения
    NONE = 0
    READ = 1 << 0      # 1
    WRITE = 1 << 1     # 2
    DELETE = 1 << 2    # 4
    ADMIN = 1 << 3     # 8
    
    # Комплексные разрешения
    USER_MANAGE = READ | WRITE | DELETE  # 7
    FULL_ACCESS = READ | WRITE | DELETE | ADMIN  # 15

class RoleModel:
    name: Mapped[str] = mapped_column(String(50), unique=True)
    permissions: Mapped[int] = mapped_column(Integer, default=Permission.NONE)
    
    # Проверка разрешений
    def has_permission(self, permission: int) -> bool:
        return bool(self.permissions & permission)
```

### Предопределенные роли

| Роль | Разрешения | Описание |
|------|-------------|----------|
| **User** | READ | Базовый пользователь |
| **Moderator** | READ, WRITE | Модератор контента |
| **Admin** | USER_MANAGE | Администратор пользователей |
| **SuperAdmin** | FULL_ACCESS | Суперадминистратор |

### Множественные роли

```python
class User:
    roles: Mapped[List["RoleModel"]] = relationship(
        "RoleModel",
        secondary="user_roles",
        back_populates="users"
    )
    
    @property
    def all_permissions(self) -> int:
        # Объединение разрешений всех ролей
        result = Permission.NONE
        for role in self.roles:
            result |= role.permissions
        return result
    
    def has_permission(self, permission: int) -> bool:
        # Проверка с учетом всех ролей
        return bool(self.all_permissions & permission)
```

## 🛡️ Middleware безопасности

### Security Headers

```python
class SecurityHeadersMiddleware:
    async def __call__(self, request: Request, call_next):
        response = await call_next(request)
        
        # Основные security headers
        response.headers["X-Content-Type-Options"] = "nosniff"
        response.headers["X-Frame-Options"] = "DENY"
        response.headers["X-XSS-Protection"] = "1; mode=block"
        response.headers["Strict-Transport-Security"] = "max-age=31536000; includeSubDomains"
        response.headers["Content-Security-Policy"] = "default-src 'self'"
        
        return response
```

### CORS Configuration

```python
class CORSMiddleware:
    def __init__(self, config: CORSConfig):
        self.allowed_origins = config.allowed_origins
        self.allowed_methods = ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
        self.allowed_headers = ["Authorization", "Content-Type", "X-Requested-With"]
        self.allow_credentials = True
        self.max_age = 86400  # 24 часа
```

## 📊 Логирование безопасности

### Структурированное логирование

```python
import structlog

logger = structlog.get_logger()

class SecurityLogger:
    def log_login_attempt(self, username: str, ip_address: str, success: bool):
        logger.info(
            "Login attempt",
            username=username,
            ip_address=ip_address,
            success=success,
            event_type="auth.login_attempt",
            risk_level="low" if success else "medium"
        )
    
    def log_security_violation(self, violation_type: str, details: dict):
        logger.warning(
            "Security violation",
            violation_type=violation_type,
            details=details,
            event_type="security.violation",
            risk_level="high"
        )
    
    def log_admin_action(self, admin_id: str, action: str, target: str):
        logger.info(
            "Admin action",
            admin_id=admin_id,
            action=action,
            target=target,
            event_type="admin.action"
        )
```

### События безопасности

| Событие | Уровень | Описание |
|---------|---------|----------|
| **Login Success** | INFO | Успешный вход |
| **Login Failure** | WARNING | Неудачная попытка входа |
| **Account Locked** | WARNING | Блокировка аккаунта |
| **IP Blocked** | WARNING | Блокировка IP |
| **Suspicious Activity** | ERROR | Подозрительная активность |
| **Privilege Escalation** | CRITICAL | Повышение привилегий |
| **Data Breach** | CRITICAL | Утечка данных |

## 🔔 Система уведомлений

### Alert Management

```python
class SecurityAlertManager:
    def send_security_alert(self, alert_type: str, details: dict):
        # Email уведомление администраторам
        if alert_type in ["account_compromised", "mass_attack"]:
            self._send_email_alert(details)
        
        # Slack уведомление команды безопасности
        if alert_type in ["suspicious_activity", "ip_blocked"]:
            self._send_slack_alert(details)
        
        # SMS для критических событий
        if alert_type == "data_breach":
            self._send_sms_alert(details)
```

### Типы уведомлений

- 📧 **Email**: Детальные отчеты администраторам
- 💬 **Slack**: Быстрые уведомления команды
- 📱 **SMS**: Критические события для ответственных лиц
- 📊 **Dashboard**: Реальное время мониторинга

## 🚨 Incident Response

### Автоматический ответ на инциденты

```python
class IncidentResponse:
    def handle_security_incident(self, incident_type: str, details: dict):
        if incident_type == "brute_force":
            self._auto_block_ip(details["ip_address"])
            self._notify_security_team("Brute force attack detected")
            
        elif incident_type == "account_compromise":
            self._auto_lock_account(details["user_id"])
            self._force_password_reset(details["user_id"])
            self._notify_user(details["user_id"], "Security alert")
            
        elif incident_type == "data_breach":
            self._isolate_system()
            self._notify_legal_team()
            self._preserve_evidence()
```

### План реагирования

1. **Detection**: Автоматическая детекция угроз
2. **Assessment**: Оценка уровня угрозы
3. **Containment**: Сдерживание угрозы
4. **Investigation**: Расследование инцидента
5. **Recovery**: Восстановление системы
6. **Lessons Learned**: Анализ и улучшения

## 🔐 Best Practices

### Для разработчиков

- ✅ **Валидация входных данных**: Всегда проверяйте и очищайте входные данные
- ✅ **Принцип минимальных привилегий**: Предоставляйте только необходимые права
- ✅ **Безопасное хранение секретов**: Используйте environment variables
- ✅ **Регулярные обновления**: Поддерживайте зависимости в актуальном состоянии
- ✅ **Код ревью**: Обязательный review изменений безопасности

### Для администраторов

- ✅ **Мониторинг логов**: Регулярно проверяйте логи безопасности
- ✅ **Обновление правил**: Актуализируйте правила детекции угроз
- ✅ **Резервное копирование**: Регулярно создавайте бэкапы
- ✅ **Тестирование**: Проводите penetration testing
- ✅ **Обучение пользователей**: Обучайте пользователей принципам безопасности

## 📋 Compliance

### Соответствие стандартам

- **GDPR**: Защита персональных данных
- **SOX**: Контроль доступа к финансовым данным
- **ISO 27001**: Система управления информационной безопасностью
- **NIST**: Рамки кибербезопасности

### Аудит безопасности

```python
class SecurityAudit:
    def generate_audit_report(self, start_date: datetime, end_date: datetime) -> dict:
        return {
            "period": f"{start_date} - {end_date}",
            "total_login_attempts": self._count_login_attempts(start_date, end_date),
            "failed_attempts": self._count_failed_attempts(start_date, end_date),
            "blocked_ips": self._count_blocked_ips(start_date, end_date),
            "security_violations": self._count_violations(start_date, end_date),
            "recommendations": self._generate_recommendations()
        }
```

## 🚀 Производительность безопасности

### Оптимизация проверок безопасности

- **Кэширование**: Redis для быстрой проверки блокировок
- **Асинхронность**: Неблокирующие проверки безопасности
- **Batch Processing**: Пакетная обработка логов
- **CDN**: Кэширование статических ресурсов

### Мониторинг производительности

- **Response Time**: Время проверок безопасности < 100ms
- **Throughput**: Обработка 10,000 запросов/сек
- **Memory Usage**: Оптимизированное использование памяти
- **Database Load**: Минимизация запросов к БД

---

## 🎯 Заключение

Система безопасности User Service обеспечивает **комплексную защиту** на всех уровнях:

🛡️ **Многоуровневая защита** от внешних угроз
🔐 **Надежная аутентификация** с современными токенами
👑 **Гибкая система авторизации** с детальным контролем доступа
📊 **Продвинутый мониторинг** с автоматическим реагированием
📈 **Масштабируемая архитектура** для роста нагрузки

Система готова к **production использованию** и соответствует современным стандартам безопасности корпоративного уровня.